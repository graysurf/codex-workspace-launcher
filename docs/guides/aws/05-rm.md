# `aws rm`

Removes workspace containers (and their named volumes).

## Remove one workspace

```sh
aws rm <name|container> --yes
```

## Remove all workspaces (danger)

```sh
aws rm --all --yes
```

This removes **all** containers labeled as codex workspaces on your host. Double-check with `aws ls` first.
