{
  "smoke": [
    {
      "name": "help",
      "command": ["bash", "scripts/bump_versions.sh", "--help"],
      "timeout_sec": 10,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "(?s)usage:.*scripts/bump_versions\\.sh"
      }
    }
  ]
}
